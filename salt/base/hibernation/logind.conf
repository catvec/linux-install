# Systemd logind configuration for suspend-then-hibernate
# This configures the system to use suspend-then-hibernate mode when the lid is closed

[Login]
# Use suspend-then-hibernate instead of plain suspend when lid is closed
HandleLidSwitch=suspend-then-hibernate
HandleLidSwitchExternalPower=suspend-then-hibernate
