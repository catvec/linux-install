agent_skills:
  # Git repo URL
  git_repo: git@git.sr.ht:~catvec/agent-skills

  # Directory relative to a user's home where the repo will be cloned
  home_relative_dir: .agents/skills

  # Keys of the pillar.users.users object of users to clone down the repo
  users:
    - noah
