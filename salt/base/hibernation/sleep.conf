# Systemd sleep configuration for suspend-then-hibernate
# This sets the delay before transitioning from suspend to hibernate

[Sleep]
# Wait 20 minutes (1200 seconds) in suspend before hibernating
HibernateDelaySec=1200
